<%= form_for(account) do |f| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fine %>
    <%= f.number_field :fine %>
  </div>

  <div class="field">
    <%= f.label :leaves %>
    <%= f.number_field :leaves %>
  </div>

  <div class="field">
    <%= f.label :last_month_adjustment %>
    <%= f.text_field :last_month_adjustment %>
  </div>

  <div class="field">
    <%= f.label :resident_id %>
    <%= f.text_field :resident_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
